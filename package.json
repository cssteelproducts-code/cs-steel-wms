{
  "name": "cs-steel-wms",
  "version": "1.0.0",
  "description": "CS Steel Products WMS - Warehouse Management System",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "copy:build": "xcopy /E /I /Y frontend\\dist\\* backend\\public\\",
    "copy:build:linux": "mkdir -p backend/public && cp -r frontend/dist/. backend/public/",
    "setup": "npm run install:all && npm run build && npm run copy:build",
    "setup:linux": "npm run install:all && npm run build && npm run copy:build:linux",
    "start": "cd backend && npm start",
    "init-db": "cd backend && npm run init-db"
  }
}
